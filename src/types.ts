export interface Employee {
  id: string;
  firstName: string;
  lastName: string;
  phone: string;
  email: string;
  title: string;
  website: string;
  address: string;
  department: string;
  telegram?: string;
  whatsapp?: string;
  maxMessenger?: boolean;
}
